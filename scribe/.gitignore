node_modules/
.env
.env.local
dist/
*.log
.DS_Store

# Local JSON stores (prospects, pipeline, sequences, meetings, clients, mailbox)
data/
# ...but keep the case-study example so the feature is discoverable in the repo.
!data/case-studies/
!data/case-studies/_example.json

# Python / Playwright
__pycache__/
*.pyc
.venv/
